{
  "name": "riomedica-android-app",
  "version": "1.0.0",
  "description": "Riomedica Healthcare - Native Android Mobile App powered by Capacitor",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "build:web": "cd ../client && npm run build",
    "build:android": "npm run build:web && cap sync android && cap open android",
    "run:android": "cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/haptics": "^6.0.0",
    "@capacitor/keyboard": "^6.0.0",
    "@capacitor/splash-screen": "^6.0.0",
    "@capacitor/status-bar": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "typescript": "^6.0.3"
  }
}
